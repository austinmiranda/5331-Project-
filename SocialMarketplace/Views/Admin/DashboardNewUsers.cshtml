@model SocialMarketplace.Models.ViewModels.Admin.DashboardSeriesViewModel
@{
    ViewBag.Title = "New Users";
    Layout = null;
}

<h2>@ViewBag.Title</h2>

@{
    var myChart = new Chart(width: 600, height: 400)
        .AddTitle("New Users")
        .AddSeries(
            name: "Default",
            chartType: "Line",
            xValue: Model.Xvalues,
            yValues: Model.Yvalues)
        .Write();
}